# wardrobe-ai-server

Backend API for Wardrobe AI.

## Node.js

Run the server under **Node 22 LTS** (`server/.nvmrc`). Native `better-sqlite3` crashes on Node 24+.

```bash
cd server
nvm use
npm run dev
```
